# azure-pipelines/teardown.yml
# Manual-only teardown pipeline for the MAPAQ App Insights .NET 10 bilingual workshop.
# Authenticates via Workload Identity Federation (WIF) using the dedicated
# `sc-workshop-teardown` service connection (separate from deploy for blast-radius isolation).
# Runs `azd env select` then `azd down --purge --force` against the named environment.

trigger: none
pr: none

pool:
  vmImage: ubuntu-latest

parameters:
  - name: azureEnvName
    displayName: Azure Developer CLI environment name to tear down (required)
    type: string

stages:
  - stage: Teardown
    displayName: Teardown azd environment
    jobs:
      - deployment: teardown_workshop
        displayName: azd down --purge --force
        environment: workshop-teardown
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self

                - task: AzureCLI@2
                  displayName: azd env select + azd down --purge --force (WIF)
                  env:
                    # azd's azure-pipelines federated provider exchanges this token for an Entra access token.
                    SYSTEM_ACCESSTOKEN: $(System.AccessToken)
                    AZURESUBSCRIPTION_SERVICE_CONNECTION_ID: sc-workshop-teardown
                  inputs:
                    connectedServiceNameARM: 'sc-workshop-teardown'
                    azureSubscription: 'sc-workshop-teardown'
                    scriptType: bash
                    scriptLocation: inlineScript
                    addSpnToEnvironment: true
                    inlineScript: |
                      set -euo pipefail

                      # Install Azure Developer CLI (azd) on the agent.
                      curl -fsSL https://aka.ms/install-azd.sh | bash
                      azd version

                      # WIF identity exposed by addSpnToEnvironment: true
                      export AZURE_CLIENT_ID="$servicePrincipalId"
                      export AZURE_TENANT_ID="$tenantId"
                      export AZURE_SUBSCRIPTION_ID="$(az account show --query id -o tsv)"

                      azd auth login \
                        --client-id "$AZURE_CLIENT_ID" \
                        --tenant-id "$AZURE_TENANT_ID" \
                        --federated-credential-provider azure-pipelines

                      AZD_ENV_NAME="${{ parameters.azureEnvName }}"

                      # Re-hydrate or create the azd env so 'azd down' has state to work with.
                      azd env select "$AZD_ENV_NAME" || \
                        azd env new "$AZD_ENV_NAME" \
                          --no-prompt \
                          --location "$(az account list-locations --query "[0].name" -o tsv)" \
                          --subscription "$AZURE_SUBSCRIPTION_ID"

                      # Best-effort azd down; tolerate missing state.
                      azd down --environment "$AZD_ENV_NAME" --purge --force --no-prompt || true

                      # Belt-and-braces: force-delete the resource group if azd state was missing
                      # or partial. No-op when the RG is already gone.
                      az group delete --name "rg-$AZD_ENV_NAME" --yes --no-wait || true
